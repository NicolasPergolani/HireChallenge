apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: notes-app
spec:
  rules:
    - host: YOUR_DOMAIN
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: notes-app
                port:
                  number: 80
